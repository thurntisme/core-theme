const prefix = '/landing';

const LANDING_ENDPOINTS = {
  FEATURED_PROJECTS: `${prefix}/projects`,
  RECENT_BLOG: `${prefix}/blog`,
  IMAGE_SHARE: `${prefix}/image-share`,
};
export default LANDING_ENDPOINTS;
